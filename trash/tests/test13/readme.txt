Изменения:

- теперь print_char(); может не только обрабатывать \n, но и \t и \b

- из-за этого read_char(); теперь умеет обрабатывать Tab и Backspace

- улучшение кода


Дополнительно:

- мысли о том, что нужно разделить ответственность, типа, чтобы в kmain не работать с буфером, то добавить input(), который бы так же принимал бы строку, только до 32 символов; суть в том, что сейчас я отлавливаю через read_char(); символы, а уже на основе того что за символ я им управляю, от чего и создаётся неправильная ответственность - \b затирает последний символ эмулируя backspace и ломая буфер, в то время как я хочу сделать надстройку над teakeyboard.h, чтобы там уже внутри обрабатывались символы и просто выводились, если это не спец символы, а если это спец символы, то \b выводился как затирающий, но при этом в буффере обрабатывался бы как сдвигающий буффер на значение левее, не убирая символ и не создавая ошибки, так же и \n чтобы обрабатывался как сейчас и записывался бы как \0, но сам по себе бы обрабатывал Enter как возвращение буфера функции в виде строки
