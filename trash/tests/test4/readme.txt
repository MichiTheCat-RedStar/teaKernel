Попытка сделать базовые функции на C:

А ведь я не могу открыть VSCode, так как он слишком требовательный, поэтому я использую Geany обычно, но тут Geany бы дал только подсвктку текста, которую и так даёт Mouspad... Я к тому, что пишу код в блокноте)

Актуальная цель по идее очень простая, разве что я не знаю как в C делаются аргументы по умолчанию: я хочу сделать для print_string аргумент color, чтобы можно было вставить байт (0x00-0xFF) и символ красился в нужный цвет (в сыром железе есть поддержка этого, там старший и младший ниббл отвечают за фоновый и текстовый цвет, я уже разбирал это в прошлых тестах ещё на моменте с ассемблером)

Если всё получится, то следующее что я буду делать - это input функцию...
Так же я почистил старые комментарии, не знаю насколько это хорошо, ведь с одной стороны я удаляю и полезные комментарии (вероятно), но и удаляю мусор, что мне нужен был только на моменты тестирования


Сейчас во время написания кода пришла идея не делать так, чтобы в функции нужно было указывать цвет курсора... Я думал сделать так, чтобы print_string() вызывала функцию print_colored_string(), только где уже аргумент color стоит как 0x0F, а print_colored_string() чтобы не отличалась print_string(), только изменил бы print_char(), добавив бы аргумент color, который бы и отправлял бы print_colored_string(), но увидел, что cursor_color является глобальной переменной и подумал, что я же могу не делать функцию для этого, а менять глобальную переменную...
В общем это вопрос между тем, какой оставить синтаксис:

```c
print_colored_string("White on Black", 0x0F);
print_colored_string("Red on Green", 0x24);
print_string("Basic text: White on Black");
```

```c
print_string("White on Black");
set_color(0x24);
print_string("Red on Green");
```

И вот второй вариант мне показался симпатичнее, он больше мне напоминает логику mlog из mindustry, к которой я стремился


Так же, чтобы всё не ограничивалось тем, что я просто написал одну простую функцию, то я решил вынести стандартную библиотеку в отдельный файл teavideo.h


По идее должно работать, проверяю...
'''
nasm -f elf32 boot.asm -o boot.o
gcc -m32 -ffreestanding -nostdlib -Wall -Wextra -Itealibs -c kernel.c -o kernel.o
gcc -m32 -ffreestanding -nostdlib -Wall -Wextra -Itealibs -c tealibs/teavideo.c -o tealibs/teavideo.o
ld -m elf_i386 -T linker.ld -o kernel.elf boot.o kernel.o tealibs/teavideo.o
qemu-system-i386 -kernel kernel.elf
'''


Итог:
Всё работает! Фото загружено в ../images!)


Изменено:
Создал src, куда разместил эту версию как "Полую", ведь она ещё ничего не умеет, но желающие уже могли бы на ней побаловаться и создать что-то своё, да и теперь есть на что опираться, а не одни только trash/ версии и тесты
