Это не совсем стандартный roadmap - тут нет списка целей, которые я явно буду выполнять, скорее это что-то вроде вектора развития, который явно может меняться по мере развития проекта:


[✓]/[◎]/[?]/[○]/[✗];

[✓] - Создание папки для тестов: ./trash/tests/test*/

[✓] - Создание папки для хранения фото тестов: ./trash/tests/images/

[✓] - Лучшее изучение Nasm и ассемблера в целом

[✓] - Понимание программирования на 6502 и сокращений

[✓] - Понимание что Nasm - это один из ассемблеров под x86

[✓] - Ещё больше страха от понимания того, что всё является абстракции на уровнях выше

[✓] - Переход с Nasm на C

[✓] - Создание функций под C, попытки реализовать вывод

[✓] - Реализовать src/ с readme для живых людей

[◎] - Создать python-скрипт для автоматизации создания дерева файлов в readme.txt

[✓] - Улучшить readme.txt в корневой папке, а иерархию файлов вынести в tree.txt

[○] - Создание "Мягкого" Python скрипта, задача которого заменить Makefile и build.bat на универсальный способ, который бы при ошибке не просто указывал бы что не так, а при нехватке компилятора конкретно бы указывало где скачать, с ссылками для Windows и предложением ввести bash для Linux

[◎] - Ядро было запущено на реальном железе и выдавало ошибку "Консоли" - надо разобраться в чём дело

[✓] - Создание функций под C, попытки реализовать ввод

[✓] - Создание ioport для чтения сканкодов

[✓] - Чтение PS/2, пока что без IRQ1 прерываний

[✓] - Перевод сканкодов в ASCII по set 1

[✓] - Добавление скан-кодов в буфер, пока с игнорирование "Отпускающих" скан-кодов и модификаторов Shift, Ctrl, etc.

[✓] - Создание input функции как надстройку над teakeyboard.h

[○] - Создание чтения с IRQ1 прерываниями, чтобы не нагружать процессор

[✓] - Создание чего-то интерактивного

[✓] - Ещё больше понять C и быть шокированным

[○] - Обработка Shift (через обработку if (scan == 0) {} else {...) // думаю по roadmdp ясно как глубоко я углубился в эту тему, прочитав все 65536 для x86

[✓] - Обработку Backspace

[✓] - /t, /b для char (в дальнейшем можно подвязать их вызов к Tab и Backspace

[✓] - readme для всех базовых tealibs

[✓] - Изменение teakeyboard.h для того, чтобы он внутри использовал teaio.h, а не связывать его с traio.h внутри kmain.c

[○] - Изменение вывода, чтобы при переполнении строки символы не вызывали clear_screen();, а переносили строки на одну вверх, стирая верхнюю, либо лучше сделать для этого отдельную функцию, чтобы она вызывалась в print_char(); вместо clear_screen();, что позволило бы удобно их менять местами в коде при желании

[◎] - Навести порядок в модулях, чтобы не было переизбыточных (Как сейчас с print, который появился только из желания сделать mlog) (модуль != API или подобное - teavideo.c работает с VGA и необходит, а teakeyboard.c вроде хорошая обёртка для создания ввода с прерыванием, только не имеет смысла для ядра, а в ring 3 и API надо делать отдельные инструменты и как итог мы будем иметь бесполезные библиотеки) (сейчас всё имеет смысл, так как находится не в "Релизе", а в trash/tests/ - то есть создано специально для тесто и эусперементов, а в дальнейшем будет убрано, как в своё время выводимый через ассемблер текст, когда можно было перейти на C, что я по итогу и сделал)

[○] - Проба смены кодовой страницы VGA | Смена раскладки для кириллицы

[◎] - Поддержка и улучшение GitHub Pages

[○] - Добавление ссылок на свои ресурсы в GitHub Pages

[○] - Сделать возможным выбор какое ядро запустить в v86 и добавить на страницу иконки GitHub, Telegram и прочие, которые бы вели на мои ресурсы (я понимаю как сделать первое как бэкендер - я просто меняю "cdrom: ***" строчку, однако я не знаю как сделать удобный интерфейс для конкретного выбора... Это позволило бы мне прямо в браузере показывать как именно МЕНЯЛОСЬ ядро, а не просто относительно-актуальную версию -> нужна помощь фронтендера и оставить на него ссылки в readme.txt в пункте о сторонней помощи)

[○] - Разделение ответственности на ядро и ОС

[○] - Микроядро, чтобы давать инструменты для работы ОС, возможно прослойка

[○] - Теста ядра в режиме ring 0 сделать возможность двигаться по 2D массиву на стрелочки и сделать что-то интерактивное на этой основе, например игру

[○] - Сделать переключение в ring 0 -> ring 3 и наоборот

[○] - Сделать загрузчик, уйти от режима вечного LiveUSB

[○] - Начать работать с ПЗУ

[✓] - Разобраться в принципе работы Линковки

[✓] - Разобраться в принципе работы Makefile

[○] - Создать простые функции для работы со строками (strip, сравнение и прочее, начать с малого, а в дальнейшем можно будет расширять и  добавить upper и lower и прочее)

[○] - Сделать подобие регулярок (простой парсинг для следующего пункта)

[✗] - Изменить teaprint чтобы он был ещё больше похож на mlog: сейчас set_color() меняет цвет глобально, поэтому меняется весь текст для printflush(), а я хочу реализовать mlog подобную смену цвета, чтобы при "[RED]Hello [WHITE]World!" цвет менялся у следующих символов после команды.. Думаю о простом парсинге - находить '[' и добавлять символы до ']' в буфер, а если словго совпадает со словом из таблицы, то сменить цвет следующим символам, но так же это требует изменения работы буфера в плане цветов или прямое его обращение к 0xB8000 $-> Удалено из-за отказа от teaprint

[✗] - Создать свой std на C, не для ядра, а чтобы понять принцип работы C и не зависеть от std* libc (Фактически она будет привязана к Linux, так как не -ffreestanding)

[✓] - Поддерживать или продолжать поддерживать код таким, чтобы программирование напоминало mlog из Mindustry (Пример: изменение цвета текста следующего сообщения через изменение глобальной переменной вместо аргумента функции, который бы менял цвет конкретного текста)

[○] - Создать таймер

[✓] - Создать мерцающий I для коретки в тексте $-> Сделано в виде '_' в io/teainput

[○] - Создание стандартных библиотек под арифметику | Работа с арифметикой

[○] - Создать простой калькулятор

[✗] - Избавление от GRUB

[?] - Перейти на Nim, так как его я знаю явно лучше C, но стоит это ещё обдумать

[✓] - Создать Makefile

[○] - Создать API с инструментами, чтобы можно было делать софт для ядра

[✓] - Научиться создавать ISO образы (добавить создание ISO в Makefle)

[✓] - Создать ISO образ

[✓] - Создать тестовое ядро под GitHub Pages

[◎] - Разобраться в принципе работе ядер в общем, понять что мне надо сделать, чтобы была полноценная ОС

[○] - Динамическая память (Heap)

[○] - Работа с хотя бы Fat12/16/32

[○] - Простейший графический режим

[○] - Написать инструмент на Python, который бы позволял бы создавать шрифты в моём формате (пока думаю, что формат бы хранил в себе символы в виде последовательности байт, которые через питон будет удобно кодировать, ведь он бы превращал 2D битмап из двоичного кода в последовательность hex значений)

[?] - Разобраться как настроить GCC на свою ОС | Интегрировать GCC в свою ОС | Сделать свой ЯП, который бы компилировался в -ffreestanding C и автоматически бы линковался или собирал бы ядро - своего рода IDE для создания ядра

[○] - Многозадачность
